\ProvidesPackage{upatras-thesis}

% Remove lipsum package when you start writing your own thesis
\usepackage{lipsum}
\usepackage{datetime}
\usepackage{wrapfig}
\usepackage{enumerate}
\usepackage{float}
\usepackage{graphicx}
\usepackage{caption}
\usepackage[acronym]{glossaries}
\usepackage{glossaries-polyglossia}
\usepackage[nottoc]{tocbibind}
\usepackage{siunitx}
\usepackage{subcaption}
\usepackage{geometry}
\usepackage{fancyhdr}
% \usepackage{cite}
\usepackage{subfiles}
\usepackage{amsmath}
\usepackage{array,tabularx}
\usepackage{chngcntr}
\usepackage{afterpage}
\usepackage{ulem}
\usepackage{polyglossia}
\usepackage{fontspec}
\usepackage{setspace} % Added for one-half line spacing style
\usepackage{hyperref}
\usepackage[
    backend=biber,
    style=numeric
]{biblatex}


\setlength{\parindent}{2em}
\onehalfspacing % Activates the 1.5 line spacing style seen in the PDF

\geometry{
	a4paper,
	left=35mm,  % Increased for clean book binding margin
	right=25mm,
	top=30mm,
	bottom=30mm
}

% PDF language settings
\setmainlanguage{greek}
\setotherlanguage[variant=british]{english}
\setmainfont{Times New Roman}
\newfontfamily\greekfont{Times New Roman}
\newfontfamily\englishfont{Times New Roman}

\hypersetup{
    colorlinks=true,
    linkcolor=black,
    filecolor=black,
    urlcolor=blue,
	citecolor=black,
}

%*******************************%
%					FANCY HDR							%
%*******************************%
\pagestyle{fancy}
\newcommand{\creator}{Georgios Tsavos}
\renewcommand{\chaptermark}[1]{\markboth{#1}{}}
\renewcommand{\sectionmark}[1]{\markright{#1}{}}

\fancyhf{}
\fancyhead[LE]{\slshape \leftmark}
\fancyhead[RO]{\slshape \rightmark}
\fancyfoot[C]{\thepage} % Clean bottom-center page numbering matching the PDF

\newtheorem{theorem}{Θεώρημα}[section]

\newdateformat{monthyeardate}{%
  \monthname[\THEMONTH], \THEYEAR}

\graphicspath{{images/}}

\newcommand{\clearemptydoublepage}{
    \newpage{\pagestyle{empty}
    \cleardoublepage}
}

\newenvironment{conditions}
  {\par\vspace{\abovedisplayskip}\noindent\begin{tabular}{>{$}l<{$} @{${}={}$} l}}
  {\end{tabular}\par\vspace{\belowdisplayskip}}

%*******************************%
%					CHAPTER FORMAT				%
%*******************************%
\makeatletter
% Adjusted to perfectly match the large multi-line Chapter headings style of the PDF
\def\@makechapterhead#1{%
  \vspace*{20\p@}%
  {\parindent \z@ \raggedright \normalfont
    \ifnum \c@secnumdepth >\m@ne
        \huge\bfseries \textgreek{Κεφάλαιο} \thechapter\par\nobreak
        \vskip 10\p@
    \fi
    \interlinepenalty\@M
    \Huge \bfseries #1\par\nobreak
    \vskip 30\p@
  }}

\def\@makeschapterhead#1{%
  \vspace*{40\p@}%
  {\parindent \z@ \raggedright \normalfont
    \interlinepenalty\@M
    \Huge \bfseries #1\par\nobreak
    \vskip 30\p@
  }}
\makeatother